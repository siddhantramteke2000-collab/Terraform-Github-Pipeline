name: sample pipeline

on:
    workflow_dispatch:
env:
    NAME: Infosys

jobs:
    job1:
     env:
        BRANCH: Pune
     runs-on: ubuntu-vm
     
    
     steps:
        - name: Company's Name
          run: echo $NAME

        - name:
          run: echo $BRANCH
        
        - name:
          env:
            CITY: Nagpur
          run: echo $CITY
